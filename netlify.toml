[build]
command = "npm run build"
publish = "dist"

[functions]
directory = "supabase/functions-Ai-trip-builder"
[build]
command = "npm run build"
publish = "dist"

[functions]
directory = "supabase/functions-Ai-trip-builder"
